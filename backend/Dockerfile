# Backend placeholder — substituído em T2/T3
FROM node:20-alpine

WORKDIR /app

# T2: copiar package.json, instalar deps, copiar src
# Por ora só mantém o container vivo para dev

CMD ["tail", "-f", "/dev/null"]
